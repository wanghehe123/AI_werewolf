# AI Werewolf Redis Configuration
# Local development defaults for Redis connection.
# Environment variable overrides take precedence (see config/redis_config.py).
redis:
  host: localhost
  port: 6379
  db: 0
  password: null
  socket_timeout: 5
  socket_connect_timeout: 5
  pool_size: 10
  key_prefix: "wolf:"
