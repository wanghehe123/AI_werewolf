[project]
name = "ai-werewolf"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic>=2",
  "sqlmodel",
  "langgraph",
  "pytest",
  "pytest-asyncio",
  "httpx"
]

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
